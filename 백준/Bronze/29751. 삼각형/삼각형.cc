#include <iostream>
#include <iomanip>

using namespace std;

int main(void){
    float a,b;
    cin>>a>>b;
    float k = a*b/2;
    
    cout<<fixed;
    cout<<setprecision(1);
    cout<<k;
    
}